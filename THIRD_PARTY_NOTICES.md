# Third-Party Notices

## 项目原创性声明

Survey Platform（万级智能体模拟问卷平台）为**从零开始独立设计与开发**的原创项目：

- 全部代码（后端 `backend/`、前端 `frontend/`、部署 `deploy/`、压测 `tests/load/`）均为本项目原创编写，**未复制、未改编、未翻译任何第三方项目的源代码**。
- 项目不包含、不部署、不依赖任何上游开源项目的交付物。
- 因此本项目**不附带任何第三方代码的版权与许可义务**。

## 项目许可证

本项目整体以 **GPL-3.0-or-later** 开源发布，完整许可文本见根目录 [`LICENSE`](./LICENSE)。

## 第三方运行时依赖

项目运行时依赖的开源库（FastAPI、React 等）均通过标准包管理器（uv / npm）按其各自许可证安装使用，属正常依赖关系，不构成代码改编。各依赖的许可证以其官方发布为准。
